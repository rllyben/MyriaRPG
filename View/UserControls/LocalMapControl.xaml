<UserControl x:Class="MyriaRPG.View.UserControls.LocalMapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="#182026" CornerRadius="12" ClipToBounds="True">
        <!-- Transparent Grid fills the Border and is the drag/zoom target -->
        <Grid x:Name="Viewport"
              Background="Transparent"
              Cursor="Hand"
              MouseLeftButtonDown="Viewport_MouseDown"
              MouseMove="Viewport_MouseMove"
              MouseLeftButtonUp="Viewport_MouseUp"
              MouseLeave="Viewport_MouseLeave"
              MouseWheel="Viewport_MouseWheel">
            <Canvas x:Name="Layer" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Canvas.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="ScaleXform" ScaleX="1" ScaleY="1"/>
                        <TranslateTransform x:Name="TranslateXform" X="0" Y="0"/>
                    </TransformGroup>
                </Canvas.RenderTransform>
            </Canvas>
        </Grid>
    </Border>
</UserControl>
