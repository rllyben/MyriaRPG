<Window x:Class="MyriaRPG.View.Windows.Window_InitError"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Initialization Failed"
        Width="540" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A2E"
        ResizeMode="NoResize">
    <Border Padding="24" Background="#1A1A2E">
        <StackPanel>
            <TextBlock Text="Failed to start MyriaRPG"
                       FontSize="20" FontWeight="Bold"
                       Foreground="#E05555" Margin="0,0,0,8"/>

            <TextBlock Text="An error occurred during initialization."
                       Foreground="#AAAACC" FontSize="13" Margin="0,0,0,16"/>

            <Border Background="#11111E" CornerRadius="4" Padding="12" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Step:" Foreground="#888899" FontSize="11" Margin="0,0,0,2"/>
                    <TextBlock Text="{Binding FailedStep}"
                               Foreground="#CFCFFF" FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <Border Background="#11111E" CornerRadius="4" Padding="12" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Error:" Foreground="#888899" FontSize="11" Margin="0,0,0,2"/>
                    <ScrollViewer MaxHeight="180" HorizontalScrollBarVisibility="Disabled">
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Foreground="#E8E8FF" FontSize="12"
                                   TextWrapping="Wrap" FontFamily="Consolas"/>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close" Width="90" IsCancel="True" Click="Close_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
