<Page x:Class="MyriaRPG.View.Pages.Page_StartupMenue"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyriaRPG.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page_StartupMenue">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Column="1" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Button x:Name="btn_singlePlayer" Content="{Binding btnSingle}" Grid.Row="1" Margin="0,5,0,5" FontWeight="Bold" FontSize="16" Command="{Binding SinglePlayer}" Grid.Column="1"/>
                <Button x:Name="btn_login" Content="{Binding btnLogin}" Grid.Row="2" Margin="0,5,0,5" FontWeight="Bold" FontSize="16" Command="{Binding Login}" Grid.Column="1"/>
                <Button x:Name="btn_register" Content="{Binding btnRegister}" Grid.Row="3" Margin="0,5,0,5" FontWeight="Bold" FontSize="16" Command="{Binding Register}" Grid.Column="1"/>
                <Button x:Name="btn_settings" Content="{Binding btnSettings}" Grid.Row="4" Margin="0,5,0,5" FontSize="16" FontWeight="Bold" Command="{Binding Settings}" Grid.Column="1"/>
            </Grid>
        </Grid>
        <Frame Name="Frame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
