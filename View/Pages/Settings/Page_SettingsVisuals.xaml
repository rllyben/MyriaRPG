<Page x:Class="MyriaRPG.View.Pages.Settings.Page_SettingsVisuals"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyriaRPG.View.Pages.Settings"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page_SettingsVisuals">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <TextBlock HorizontalAlignment="Right" TextWrapping="Wrap" Text="{Binding DarkMode}" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="16" Foreground="{DynamicResource Brush.Foreground}" FontWeight="Bold"/>
        <CheckBox Grid.Column="1" Content="{Binding DarkMidnightMode}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" IsChecked="{Binding DarkModeSetter}" Background="{DynamicResource Brush.Background}" BorderBrush="{DynamicResource Brush.Border}" Foreground="{DynamicResource Brush.Foreground}"/>

    </Grid>
</Page>
